//container parameters; ensure these are OK for your system

singularity {

  enabled = "true"
  autoMounts = "true"
  cacheDir = "/data/genome/singularity"

}

//NB to set executor (ours was PBS)
process.executor = 'pbs'
